package ru.jucharick.tasks.domain;

public enum TaskStatus {
    /**
     * the status of the task
     * assigned
     * in_progress
     * resolved
     * reopened
     * closed
     */
    assigned,in_progress,resolved,reopened,closed
}
